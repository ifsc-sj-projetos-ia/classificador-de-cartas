# notebooks/

Coloque aqui o notebook de treino usado no Google Colab
(ex.: `treino_cartas_colab.ipynb`). Ele deve reproduzir o pipeline de
`src/`: preparacao dos dados, treino em 2 fases (feature extraction ->
fine-tuning), avaliacao no teste e no conjunto OOD, e geracao das figuras
em `reports/`.
